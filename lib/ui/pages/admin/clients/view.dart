import 'package:flutter/material.dart';

class AdminClients extends StatelessWidget {
  const AdminClients({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Admin Clients"),
    );
  }
}